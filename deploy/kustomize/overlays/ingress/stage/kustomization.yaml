apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/ingress

secretGenerator:
  - name: proxy-app-tls
    type: kubernetes.io/tls
    behavior: merge
    files:
      - cert/tls.crt
      - cert/tls.key

namespace: stage
